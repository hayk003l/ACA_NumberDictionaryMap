#ifndef __NUMBERDICTIONARY__
#define __NUMBERDICTIONARY__
#include <map>
#include <string>

std::string numsDictionary(const std::map<int, std::string>&, const int& num);

#endif // __NUMBERDICTIONARY__